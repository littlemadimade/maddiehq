{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "if echo \"$TOOL_INPUT\" | grep -q 'git commit'; then bash node/scripts/check-wiki-coverage.sh 2>/dev/null || true; fi"
          }
        ]
      }
    ]
  }
}
